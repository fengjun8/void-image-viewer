# Required for @opennextjs/cloudflare: its bundling step's static analysis
# is incompatible with pnpm's symlinked node_modules layout
# (causes "Dynamic require of /.next/server/middleware-manifest.json" at runtime).
# See: https://github.com/cloudflare/workers-sdk/issues/10236
node-linker=hoisted
